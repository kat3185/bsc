<%= form_for(event_volunteer_slot_availability) do |f| %>
  <% if event_volunteer_slot_availability.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event_volunteer_slot_availability.errors.count, "error") %> prohibited this event_volunteer_slot_availability from being saved:</h2>

      <ul>
      <% event_volunteer_slot_availability.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :event_id %>
    <%= f.text_field :event_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
