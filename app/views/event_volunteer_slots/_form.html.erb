<%= form_for(event_volunteer_slot) do |f| %>
  <% if event_volunteer_slot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event_volunteer_slot.errors.count, "error") %> prohibited this event_volunteer_slot from being saved:</h2>

      <ul>
      <% event_volunteer_slot.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
